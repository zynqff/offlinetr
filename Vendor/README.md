Place the generated `llama.xcframework` here.
Run `../Scripts/bootstrap_llama.sh` from this directory's parent project root.
Then add `Vendor/llama.xcframework` to the Xcode target's Frameworks, Libraries, and Embedded Content.
